# WebSideloadApp 部署指南

## 📦 构建信息

- **版本:** {{VERSION}}
- **认证状态:** {{AUTH_STATUS}}
- **构建时间:** {{BUILD_TIME}}
- **Commit:** {{COMMIT_SHA}}

{{AUTH_NOTE}}

## 快速部署选项

### 选项 1: 合并部署到单个 Worker（推荐）✨

**优势**: 
- ✅ 只需一个 Worker，费用更低
- ✅ 前后端同域名，无 CORS 问题
- ✅ 配置简单，一键部署

**部署步骤**:

```bash
# 进入后端目录
cd backend

# 安装依赖
bun install

# 配置认证（生产环境必需）
wrangler secret put ACCESS_TOKEN_HASH
# 或
wrangler secret put ACCESS_PASSWORD

# 一键部署（前端 + 后端）
npx wrangler deploy
```

**访问地址**: `https://web-sideload-app.<your-subdomain>.workers.dev`

- 前端页面: `https://xxx.workers.dev/`
- WISP 代理: `wss://xxx.workers.dev/wisp/?token=xxx`

---

### 选项 2: 分离部署（Cloudflare Pages + Workers）

**适用场景**: 需要独立的 CDN 缓存策略或前后端独立开发

#### 2.1 前端部署到 Cloudflare Pages

```bash
cd frontend/dist

# 方法 A: 使用 Wrangler CLI
npx wrangler pages deploy . --project-name=web-sideload-app

# 方法 B: 使用 Cloudflare Dashboard
# 1. 登录 https://dash.cloudflare.com
# 2. 进入 Pages > Create a project
# 3. 上传 frontend/dist 目录
```

#### 2.2 后端部署到 Cloudflare Workers

```bash
cd ../backend
bun install
wrangler secret put ACCESS_TOKEN_HASH
npx wrangler deploy
```

**注意**: 分离部署时，前端需要在构建时嵌入后端 URL

---

### 选项 3: Docker 部署

```bash
# 在 deploy-package 目录中
docker build -t web-sideload-app .
docker run -d -p 3000:3000 --name sideload web-sideload-app
```

**注意**: Docker 部署仅包含前端静态资源，不包含 WISP 代理功能。

---

### 选项 4: Nginx 静态托管

```bash
# 将 frontend/dist 目录内容复制到 Nginx 站点目录
sudo cp -r frontend/dist/* /var/www/html/
sudo systemctl restart nginx
```

**注意**: 同样需要独立的 WISP 后端服务。

## 环境变量配置

### Cloudflare Workers 必需配置

#### 方法 1: ACCESS_TOKEN_HASH(推荐生产环境)
```bash
# 生成 SHA-256 哈希
echo -n "your_password" | sha256sum

# 设置 Secret
wrangler secret put ACCESS_TOKEN_HASH
# 粘贴上面生成的哈希值
```

**优点:**
- ✅ 安全性高,不存储明文密码
- ✅ 无运行时计算开销
- ✅ 符合安全最佳实践

#### 方法 2: ACCESS_PASSWORD(简单,适合开发/测试)
```bash
wrangler secret put ACCESS_PASSWORD
# 直接输入密码
```

**优点:**
- ✅ 配置简单
- ⚠️ 首次请求有微小计算开销

**注意:** Secret 修改后立即生效,无需重新部署 Worker 代码。

### 前端配置

如果需要在构建时嵌入 WISP URL 和 Token:

```env
# frontend/.env.production
VITE_WISP_URL=wss://your-worker.workers.dev/wisp/?token=your_password
```

**重要:** 
- Token 在构建时通过 Vite 环境变量注入静态资源
- 修改 Token 后必须重新构建前端(`bun run build`)
- 前端 Token 虽在代码可见,但通过 HTTPS 传输且配合后端哈希验证

## 注意事项

### 浏览器兼容性
- ✅ Chrome 61+
- ✅ Edge 79+
- ❌ Firefox(不支持 WebUSB)
- ❌ Safari(不支持 WebUSB)

### HTTPS 要求
- WebUSB 和 Crypto API 必须在安全上下文(HTTPS 或 localhost)中运行
- 生产环境必须使用 HTTPS

### 性能
- 首次加载约 2-3 MB(WASM 模块)
- 签名耗时 5-15 秒

### 安全最佳实践
1. **生产环境必须配置认证**(ACCESS_TOKEN_HASH 或 ACCESS_PASSWORD)
2. 定期轮换 Secret 和前端 Token
3. 防范 XSS 攻击以保护内存中的凭证
4. 监控 Cloudflare Workers 使用情况,避免费用滥用

## 故障排除

| 问题 | 解决方案 |
|------|---------|
| 401 Unauthorized | 检查前后端 token 是否一致 |
| WebSocket 连接失败 | 检查 WISP_URL 是否正确 |
| 设备未检测到 | 检查 USB 线是否支持数据传输 |
| 签名失败 | 检查 Apple Developer 账户状态 |
| 前端 404 | 确保正确部署了 dist 目录 |

## 相关链接

- [项目主页](https://github.com/yueying23/sideimpactor)
- [问题反馈](https://github.com/yueying23/sideimpactor/issues)
- [Cloudflare Workers 文档](https://developers.cloudflare.com/workers/)
